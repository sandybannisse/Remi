using UnityEngine;

public class IceCreamCollector : MonoBehaviour
{

    public int iceCreamCount = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void IcecreamCollect()
    {
        iceCreamCount++;

    }
}
